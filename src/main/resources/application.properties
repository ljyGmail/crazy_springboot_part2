# db driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# db url address
spring.datasource.url=jdbc:mysql://localhost:3306/crazy_springboot?serverTimezone=UTC
# db username
spring.datasource.username=root
# db password
spring.datasource.password=445566hh

# ============================================================
# JPA related attributes
spring.jpa.database=mysql
# show SQL
spring.jpa.show-sql=true
# create table automatically
spring.jpa.generate-ddl=true
# the below config can also be used to create table automatically
# spring.jpa.hibernate.ddl-auto=update
# specify dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect